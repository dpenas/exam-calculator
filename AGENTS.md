I don't have an AI subscription, so I've used AI to help me in some limited cases:

- Generate the Dockerfile, docker-compose and composer.json based on the libraries and versions I wanted to use.
- Asked for a linear interpolation algorithm to calculate what the grade would be if they fall in between the defined percentages. Verified it myself with some manual cases.
- Asked to create some XML files for testing and to create the tests for them based on my code. I verified the implementation and its logic and refined it a bit, since there were no cases that were covering all important cases (when a student gets exactly a 5.5, for example).
- Asked about the Pearson formula in order to understand how it works, get some examples of how it works and a PHP implementation of it.
- After making the first iteration, I asked the AI to help me with general code improvements. It gave me a few good pointers about the naming of some classes and some function improvements that I applied. I verified that the logic was still intact after these changes and that the tests were still passing.
- I asked the AI to calculate the Pearson formula for the excel files that I had, so I could know the right result and create tests for it. I verified a couple of these values manually as well (and so I could really understand how this formula works).
- I gave the AI a file with the output of all the values (just the var_dump) and told it to create a file with 2 tables in order and to represent the data in a nice way. This first iteration looks good, so I didn't change anything from it!
- Asked the AI to generate a basic README file based on the commands that I use to have everything up and running and generate the output file (docker-compose up -d, composer install, etc.).
- I've given an AI the assignment (so it understand the context), the output file and the given excel file to verify that all the data in the output file is correct. All the data has been verified before in tests and some of it manually, but it's good to double check that everything makes sense!